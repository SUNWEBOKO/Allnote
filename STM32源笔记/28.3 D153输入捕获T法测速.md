# 28.3 D153 输入捕获 T 法测速：测脉冲周期

这一节讲 D153 模块的 **T 法测速**：把定时器配置成输入捕获模式，测量相邻编码器脉冲之间的时间间隔，再换算成输出轴转速。

T 法测速链路如下：

```text
编码器单相脉冲
→ TIM 输入捕获记录边沿时间
→ 相邻两次捕获值相减得到周期
→ period_ticks / timer_hz
→ rps / rpm
```

电机驱动层见 [[28.1 D153电机驱动模块]]。编码器模式 M 法测速见 [[28.2 D153编码器M法测速]]。

---

## 本节核心问题

学完这一节，应该能回答下面几个问题：

- T 法测速和 M 法测速的根本区别是什么？
- CubeMX 里怎样把定时器配置成输入捕获？
- 输入捕获测到的 `period_ticks` 怎样换算成 `rps`？
- 为什么 T 法低速分辨率高，但高速中断压力更大？
- 单相输入捕获怎样处理方向符号？
- T 法测速模块应该怎样接入 `HAL_TIM_IC_CaptureCallback()`？

---

## 1. T 法测速的基本思想

M 法是在固定时间内数脉冲，T 法反过来：

```text
不先固定时间窗，
而是测相邻两个脉冲之间隔了多久。
```

如果两个脉冲之间时间很短，说明转得快；时间很长，说明转得慢。

设相邻两次捕获差值为 `period_ticks`，输入捕获定时器计数频率为 `timer_hz`，则一个脉冲周期为：

$$
T_{\text{pulse}} = \frac{period_{\text{ticks}}}{timer_{\text{hz}}}
$$

如果输出轴一圈对应 `edges_per_output_rev` 个被捕获边沿，则：

$$
r/s = \frac{timer_{\text{hz}}}{period_{\text{ticks}} \times edges_{\text{per\_output\_rev}}}
$$

---

## 2. T 法和 M 法的区别

| 方法 | 测量对象 | 低速表现 | 高速表现 |
| --- | --- | --- | --- |
| M 法 | 固定时间内的计数增量 | 计数少，量化误差明显 | 稳定，硬件编码器计数压力小 |
| T 法 | 相邻脉冲的时间间隔 | 周期长，分辨率高 | 中断频繁，捕获处理压力增大 |

简单记法：

```text
M 法：固定时间，数了多少个脉冲
T 法：固定脉冲，等了多少时间
```

所以：

- 低速更适合 T 法
- 中高速更适合 M 法
- 工程上也可以低速用 T 法、高速用 M 法，但初学阶段先把两种方法分别调通

---

## 3. 计数口径

T 法通常只捕获一个编码器相位的一种边沿，例如 A 相上升沿。

D153 参数：

| 参数 | 数值 | 含义 |
| --- | --- | --- |
| `D153C_PPR` | 13 | 电机本体转一圈的编码器脉冲数 |
| `D153C_REDUCTION` | 30 | 减速比 30:1 |
| `D153C_T_EDGE_MULTIPLE` | 1 | 只捕获单相上升沿 |

输出轴一圈对应的捕获边沿数为：

$$
13 \times 30 \times 1 = 390
$$

如果你改成双边沿捕获，则：

$$
D153C\_T\_EDGE\_MULTIPLE = 2
$$

如果改成两相都参与捕获，则还要按真实捕获边沿数重新计算。不要把 M 法编码器模式的四倍频计数 `1560` 直接搬到 T 法里。

---

## 4. CubeMX 配置输入捕获

T 法测速和 M 法测速通常是**二选一配置**。同一个定时器通道不能同时既做 Encoder Mode，又做 Input Capture。

### 4.1 选择定时器和通道

可以按下面方式分配：

| 电机 | 捕获信号 | 定时器配置 |
| --- | --- | --- |
| A 电机 | A 相上升沿 | `TIM2_CH1` Input Capture |
| B 电机 | A 相上升沿 | `TIM4_CH1` Input Capture |

如果你还需要方向符号，可以把另一相配置成普通 GPIO 输入，在捕获中断里读取它的电平。

### 4.2 定时器基础参数

建议先把输入捕获定时器计数频率设为 1 MHz，方便换算：

| 参数 | 示例值 |
| --- | --- |
| 定时器时钟 | 72 MHz |
| PSC | 71 |
| Counter Period | 先按 `65535` 配，和当前工程的 TIM2/TIM4 口径保持一致 |
| 计数频率 | $72\text{ MHz} / (71 + 1) = 1\text{ MHz}$ |
| tick 时间 | 1 us |

如果定时器时钟不是 72 MHz，按实际时钟重新计算 `PSC`，并同步修改驱动宏里的 `D153C_T_x_TIMER_HZ`。如果以后把 TIM2 真正按 32 位全范围使用，再把 `D153C_T_A_TIM_32BIT` 改成 `true`。

### 4.3 Input Capture Channel

通道参数建议从下面配置开始：

| 配置项 | 建议值 |
| --- | --- |
| Channel | `Input Capture direct mode` |
| Polarity | `Rising Edge` |
| IC Prescaler | `No division` |
| IC Filter | 先填 0，信号抖动明显再增大 |
| NVIC | 开启对应 TIM 中断 |

如果编码器线较长或干扰明显，可以适当增加输入滤波。滤波太大也会引入延迟，先从小值调。

---

## 5. T 法测速模块配置

头文件：

```c
#include "d153c_speed_t.h"
```

默认宏在 `d153c_speed_t.h` 中：

```c
#define D153C_T_EDGE_MULTIPLE          1.0f

#define D153C_T_A_IC_TIM              (&htim2)
#define D153C_T_A_IC_CH               TIM_CHANNEL_1
#define D153C_T_A_TIM_32BIT           false
#define D153C_T_A_TIMER_HZ            1000000.0f
#define D153C_T_A_DIR_PORT            NULL
#define D153C_T_A_DIR_PIN             0U
#define D153C_T_A_DIR_HIGH_FORWARD    true

#define D153C_T_B_IC_TIM              (&htim4)
#define D153C_T_B_IC_CH               TIM_CHANNEL_1
#define D153C_T_B_TIM_32BIT           false
#define D153C_T_B_TIMER_HZ            1000000.0f
#define D153C_T_B_DIR_PORT            NULL
#define D153C_T_B_DIR_PIN             0U
#define D153C_T_B_DIR_HIGH_FORWARD    true
```

这些宏的含义：

| 宏 | 含义 |
| --- | --- |
| `D153C_T_x_IC_TIM` | 输入捕获使用的定时器句柄 |
| `D153C_T_x_IC_CH` | 输入捕获通道 |
| `D153C_T_x_TIM_32BIT` | 定时器计数器是否为 32 位 |
| `D153C_T_x_TIMER_HZ` | 输入捕获定时器计数频率 |
| `D153C_T_x_DIR_PORT/PIN` | 可选方向相 GPIO，不需要方向时填 `NULL` |
| `D153C_T_x_DIR_HIGH_FORWARD` | 方向相为高电平时是否表示正向 |

默认不配置方向相，所以 T 法结果是非负速度。如果要带符号速度，需要把另一相编码器信号接成 GPIO 输入，并填写 `DIR_PORT` 和 `DIR_PIN`。

---

## 6. T 法测速 API

```c
void d153c_speed_t_init(void);
void d153c_speed_t_deinit(void);

void d153c_speed_t_capture_callback(TIM_HandleTypeDef *htim);
void d153c_speed_t_update_timeout(uint32_t timeout_ms);
void d153c_speed_t_invert(int id, bool inv);

float d153c_speed_t_rps(int id);
float d153c_speed_t_rpm(int id);
uint32_t d153c_speed_t_period_ticks(int id);
bool d153c_speed_t_valid(int id);
```

接口分工：

- `d153c_speed_t_init()`：启动输入捕获中断
- `d153c_speed_t_capture_callback()`：放进 HAL 输入捕获回调里，处理捕获值
- `d153c_speed_t_update_timeout()`：长时间没有新脉冲时，把速度归零
- `d153c_speed_t_rps()` / `rpm()`：读取输出轴速度
- `d153c_speed_t_period_ticks()`：读取最近一次捕获周期，方便调试

---

## 7. 推荐调用方式

### 7.1 初始化

```c
#include "d153c_motor.h"
#include "d153c_speed_t.h"

int main(void)
{
    HAL_Init();
    SystemClock_Config();

    MX_GPIO_Init();
    MX_TIM2_Init();   // A 电机输入捕获
    MX_TIM3_Init();   // PWM
    MX_TIM4_Init();   // B 电机输入捕获

    d153c_motor_init();
    d153c_speed_t_init();

    d153c_motor_drive(D153C_MOTOR_A, 300);
    d153c_motor_drive(D153C_MOTOR_B, 300);

    while (1)
    {
        d153c_speed_t_update_timeout(100);

        float ra = d153c_speed_t_rps(D153C_MOTOR_A);
        float rb = d153c_speed_t_rps(D153C_MOTOR_B);
        (void)ra;
        (void)rb;
    }
}
```

### 7.2 接入 HAL 输入捕获回调

```c
void HAL_TIM_IC_CaptureCallback(TIM_HandleTypeDef *htim)
{
    d153c_speed_t_capture_callback(htim);
}
```

只要 CubeMX 开启了 TIM 输入捕获中断，并且 `d153c_speed_t_init()` 执行了 `HAL_TIM_IC_Start_IT()`，每次捕获到边沿时都会进入这个回调。

---

## 8. 换算示例

假设：

- 输入捕获计数频率 `timer_hz = 1 MHz`
- 捕获单相上升沿，输出轴一圈对应 `390` 个捕获边沿
- 某次捕获周期 `period_ticks = 1000`

则一个脉冲周期为：

$$
T_{\text{pulse}} = \frac{1000}{1000000} = 1\text{ ms}
$$

输出轴速度为：

$$
r/s = \frac{1000000}{1000 \times 390} \approx 2.56
$$

如果 `period_ticks = 10000`：

$$
r/s = \frac{1000000}{10000 \times 390} \approx 0.256
$$

周期越大，速度越低。

---

## 9. 方向符号处理

单相输入捕获只能测频率，不能天然判断方向。

要得到带符号速度，常见方法是：

1. 捕获 A 相边沿
2. 把 B 相配置为普通 GPIO 输入
3. 在捕获中断发生时读取 B 相电平
4. 根据 A/B 相位关系判断方向

驱动模块提供了可选方向 GPIO：

```c
#define D153C_T_A_DIR_PORT            GPIOx
#define D153C_T_A_DIR_PIN             GPIO_PIN_y
#define D153C_T_A_DIR_HIGH_FORWARD    true
```

如果读出来的速度符号反了，不要改公式，直接用：

```c
d153c_speed_t_invert(D153C_MOTOR_A, true);
```

如果不配置方向 GPIO，`d153c_speed_t_rps()` 返回非负值，适合只关心速度大小的场景。

---

## 10. 超时归零

T 法在低速时会很久才等到下一次脉冲。如果电机突然停止，最后一次捕获值会一直留在模块里，所以需要超时归零。

主循环里周期性调用：

```c
d153c_speed_t_update_timeout(100);
```

含义是：超过 100 ms 没有新捕获边沿，就认为速度为 0。

超时时间要按最低可测速度选择：

- 太短：低速时会误判为 0
- 太长：电机停止后速度归零不及时

---

## 11. 常见故障

| 现象 | 优先检查项 |
| --- | --- |
| `valid` 一直是 false | 输入捕获中断是否开启，`HAL_TIM_IC_Start_IT()` 是否执行，通道宏是否匹配 |
| `period_ticks` 一直为 0 | 捕获通道没接到编码器信号，或回调没有调用 `d153c_speed_t_capture_callback()` |
| 速度偏大或偏小 | `D153C_T_x_TIMER_HZ`、`D153C_T_EDGE_MULTIPLE`、`D153C_PPR`、`D153C_REDUCTION` 是否正确 |
| 高速时系统变慢 | 捕获中断频率太高，考虑改用 M 法或降低捕获边沿数 |
| 速度符号反了 | 检查方向相 GPIO，或调用 `d153c_speed_t_invert()` |
| 停车后速度不归零 | 主循环没有调用 `d153c_speed_t_update_timeout()`，或超时时间过长 |

---

## 12. M 法和 T 法怎么选

| 场景 | 建议 |
| --- | --- |
| 中高速闭环控制 | 优先 M 法，采样稳定，中断压力小 |
| 很低速测量 | 优先 T 法，分辨率更高 |
| 只需要速度大小 | T 法可只捕获单相，不配方向 GPIO |
| 需要完整方向和速度 | M 法更直接；T 法需要额外方向相判断 |
| 初学调试 | 先单独调通一种方法，再考虑混合测速 |

最重要的是不要混用计数口径：

```text
M 法编码器模式：输出轴一圈 13 × 4 × 30 = 1560 个计数
T 法单相上升沿：输出轴一圈 13 × 1 × 30 = 390 个捕获边沿
```

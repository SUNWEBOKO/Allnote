# STM32 HAL 库：软硬件 I2C 读写 MPU6050

> 本章是《09 I2C》的递进实验。请先完成前篇 OLED 软件 I2C 的最小实验，再开始本章。你将在自己的两个 CubeMX 工程中分别复现软件 I2C 版和 HAL 硬件 I2C 版，不需要创建一个额外的“参考工程”。

## 1. 本章要完成什么

本章使用 STM32F103C8T6、MPU6050 和前篇已经验收过的 SSD1306 OLED，完成两个独立实现：

1. PB10、PB11 配置为普通 GPIO，用软件 I2C 读写 MPU6050；
2. PB10、PB11 交给 I2C2 外设，用 HAL 硬件 I2C 读写 MPU6050。

两套实现保持以下内容不变：

- OLED 显示布局；
- MPU6050 设备地址和寄存器；
- 初始化参数；
- `MPU6050_Data` 数据结构；
- 六轴数据的验收方法。

只替换 MPU6050 的底层通信方式：

```text
供电与上拉
    -> PB10/PB11 软件 I2C 或 I2C2 硬件 I2C
    -> MPU6050 地址与寄存器事务
    -> WHO_AM_I 和六轴原始数据
    -> PB8/PB9 软件 I2C OLED
    -> OLED 同屏显示
```

完成本章后，你应能解释：

- 为什么指定寄存器读取需要“写寄存器地址，再重复起始后读取”；
- 为什么软件 I2C 要在读前释放 SDA；
- 为什么最后一个读字节后发送 NACK；
- 为什么 HAL 地址参数写成 `0x68 << 1`；
- 如何把两个字节拼成有符号的 16 位数据；
- 为什么相同上层代码可以更换不同的 I2C 底层。

### 1.1 前置条件

- 已完成《09 I2C》中 OLED 单字符或字符串显示实验；
- 掌握 STM32CubeMX、GPIO、C 数组和位运算；
- 知道 I2C 的 START、STOP、ACK 和 7 位地址；
- 能编译、下载和复位 STM32 工程。

### 1.2 本章实验顺序

按以下顺序完成，不要一开始就调试六轴显示：

1. **实验一：软件 I2C 读取 `WHO_AM_I`**；
2. **实验二：软件 I2C 配置并读取六轴数据**；
3. **实验三：14 字节连续读取**；
4. **实验四：用 HAL 硬件 I2C 替换软件底层**。

每个实验都遵循：

```text
配置/加入文件 -> 最小代码 -> 下载运行 -> 观察结果 -> 验收 -> 失败后按层排查
```

---

## 2. 硬件连接

### 2.1 两组 I2C 引脚

| 模块 | 信号 | STM32F103C8T6 | 实现方式 |
| --- | --- | --- | --- |
| OLED | SCL | PB8 | GPIO 软件 I2C |
| OLED | SDA | PB9 | GPIO 软件 I2C |
| MPU6050 | SCL | PB10 | 软件版为 GPIO；硬件版为 I2C2_SCL |
| MPU6050 | SDA | PB11 | 软件版为 GPIO；硬件版为 I2C2_SDA |
| 两模块 | VCC | 3.3V | 按模块供电要求确认 |
| 两模块 | GND | GND | 必须与 STM32 共地 |

```text
PB8  ---------------- OLED SCL
PB9  ---------------- OLED SDA

PB10 ---------------- MPU6050 SCL
PB11 ---------------- MPU6050 SDA

3.3V ---------------- OLED VCC、MPU6050 VCC
GND  ---------------- OLED GND、MPU6050 GND
```

OLED 和 MPU6050 使用两组互相独立的总线。即使硬件版把 PB10/PB11 交给 I2C2，OLED 仍由 PB8/PB9 软件驱动，因此切换 MPU6050 底层时不需要修改显示端。

### 2.2 MPU6050 其余引脚

- `AD0=0` 时，MPU6050 的 7 位地址是 `0x68`；
- `AD0=1` 时，7 位地址是 `0x69`；
- 本章默认模块把 AD0 下拉，因此使用 `0x68`；
- `INT` 暂不连接，本章使用轮询读取；
- `XDA/XCL` 是辅助 I2C 接口，本章不使用。

### 2.3 上电前检查

1. OLED、MPU6050 和 STM32 共地；
2. VCC、GND 没有接反，确认模块允许 3.3 V 供电；
3. PB10 为 SCL，PB11 为 SDA；
4. PB8/PB9 与 PB10/PB11 没有接混；
5. 两组总线空闲时，SCL、SDA 都应接近 3.3 V；
6. 确认模块已有上拉，或按原理图补充合适的外部上拉；
7. 软件版不要让 PB10/PB11 同时被 I2C2 或 USART3 占用。

---

## 3. 地址和寄存器：先分清四种数值

初学者最容易把设备地址、线上地址字节和内部寄存器地址混在一起。始终使用下面的区分：

| 名称 | 示例 | 用途 |
| --- | ---: | --- |
| 7 位设备地址 | `0x68` | 表示是哪一个 MPU6050 |
| 软件 I2C 线上写地址字节 | `0xD0` | `0x68 << 1`，最低位为写方向 |
| 软件 I2C 线上读地址字节 | `0xD1` | `(0x68 << 1) | 1`，最低位为读方向 |
| HAL 地址参数 | `0x68 << 1` | 传给 `HAL_I2C_Mem_Read/Write()` |
| 内部寄存器地址 | `0x75`、`0x3B` | 指定芯片内部的寄存器 |

```c
#define MPU6050_ADDR_7BIT  0x68U
#define MPU6050_ADDR_HAL   (MPU6050_ADDR_7BIT << 1)
#define MPU6050_ADDR_WRITE (MPU6050_ADDR_7BIT << 1)
#define MPU6050_ADDR_READ  ((MPU6050_ADDR_7BIT << 1) | 0x01U)
```

注意：上表中的“软件 I2C 线上读地址字节”应写成 `((0x68 << 1) | 1)`；这里的 `|` 是按位或，不是数学加法。软件代码直接发送 `0xD0/0xD1`，HAL 接口传入左移后的 `0xD0`，不要把 `0xD0` 再左移一次。

---

## 4. 指定地址读事务

### 4.1 写一个寄存器

```text
START
-> 0xD0              地址 0x68 + 写
<- ACK
-> Register Address
<- ACK
-> Data
<- ACK
STOP
```

例如向 `PWR_MGMT_1(0x6B)` 写 `0x01`，可以解除睡眠并选择 X 轴陀螺仪 PLL 作为时钟参考。

### 4.2 读一个寄存器

读取 `WHO_AM_I(0x75)` 时，主机先用写方向设置内部寄存器地址，再用重复起始切换到读方向：

```text
START
-> 0xD0              地址 0x68 + 写
<- ACK
-> 0x75              WHO_AM_I 寄存器
<- ACK
REPEATED START
-> 0xD1              地址 0x68 + 读
<- ACK
<- 0x68              从机返回一个数据字节
-> NACK              主机声明不再读取
STOP
```

最后一个字节后的 NACK 在这里是**正常的结束动作**，不是通信失败。若读取多个字节，前面的字节后发送 ACK，最后一个字节后才发送 NACK。

### 4.3 连续读取 14 字节

从 `ACCEL_XOUT_H(0x3B)` 开始连续读取 14 字节：

| 偏移 | 数据 |
| ---: | --- |
| 0, 1 | Accel X 高、低字节 |
| 2, 3 | Accel Y 高、低字节 |
| 4, 5 | Accel Z 高、低字节 |
| 6, 7 | Temperature 高、低字节 |
| 8, 9 | Gyro X 高、低字节 |
| 10, 11 | Gyro Y 高、低字节 |
| 12, 13 | Gyro Z 高、低字节 |

连续读取减少了地址和起停条件的重复开销，也减少了逐轴读取的时间跨度。它不能表述成所有数据在模拟意义上绝对同时采样；准确说法是，MPU6050 提供了连续寄存器读取路径，主机可以更一致地取得这一组数据。

---

## 5. MPU6050 寄存器和数据格式

### 5.1 本章使用的寄存器

```c
#define MPU6050_SMPLRT_DIV      0x19U
#define MPU6050_CONFIG          0x1AU
#define MPU6050_GYRO_CONFIG     0x1BU
#define MPU6050_ACCEL_CONFIG    0x1CU
#define MPU6050_ACCEL_XOUT_H    0x3BU
#define MPU6050_GYRO_XOUT_H     0x43U
#define MPU6050_PWR_MGMT_1      0x6BU
#define MPU6050_PWR_MGMT_2      0x6CU
#define MPU6050_WHO_AM_I        0x75U

#define MPU6050_PWR_MGMT_1_VALUE  0x01U
#define MPU6050_PWR_MGMT_2_VALUE  0x00U
#define MPU6050_SMPLRT_DIV_VALUE  0x09U
#define MPU6050_CONFIG_VALUE      0x06U
#define MPU6050_GYRO_FS_2000DPS   0x18U
#define MPU6050_ACCEL_FS_16G      0x18U
```

MPU6050 上电后默认处于睡眠状态，必须先配置 `PWR_MGMT_1`，否则即使 `WHO_AM_I` 正确，测量值也可能不更新。

### 5.2 初始化配置和验证

| 寄存器 | 写入值 | 作用 | 建议验证 |
| --- | ---: | --- | --- |
| `PWR_MGMT_1` | `0x01` | 解除睡眠，选择 X 轴陀螺仪 PLL | 回读后确认不在睡眠 |
| `PWR_MGMT_2` | `0x00` | 使能全部加速度计和陀螺仪轴 | 回读值为 `0x00` |
| `SMPLRT_DIV` | `0x09` | DLPF 开启时采样率约 100 Hz | 与 `CONFIG` 一起理解 |
| `CONFIG` | `0x06` | 设置 DLPF_CFG=6 | 回读值为 `0x06` |
| `GYRO_CONFIG` | `0x18` | 陀螺仪量程 ±2000 °/s | 回读值为 `0x18` |
| `ACCEL_CONFIG` | `0x18` | 加速度计量程 ±16 g | 回读值为 `0x18` |

本章两套工程使用同一组配置。统一参数保证灵敏度和量级可对照，不保证两次不同采样时刻的每个原始值逐位相同。

### 5.3 大端、有符号数据拼接

每个轴是大端存放的 16 位二进制补码：

```c
static int16_t MPU6050_BE16(const uint8_t *p)
{
    return (int16_t)(((uint16_t)p[0] << 8) | p[1]);
}
```

高字节必须左移 8 位。先按 `uint16_t` 拼接，再转成 `int16_t` 解释符号，可以避免整数提升带来的歧义。下面是错误写法：

```c
value = high | low;
value = (high >> 8) | low;
```

### 5.4 量程换算

| 加速度量程 | 灵敏度 |
| --- | ---: |
| ±2 g | 16384 LSB/g |
| ±4 g | 8192 LSB/g |
| ±8 g | 4096 LSB/g |
| ±16 g | 2048 LSB/g |

| 陀螺仪量程 | 灵敏度 |
| --- | ---: |
| ±250 °/s | 131 LSB/(°/s) |
| ±500 °/s | 65.5 LSB/(°/s) |
| ±1000 °/s | 32.8 LSB/(°/s) |
| ±2000 °/s | 16.4 LSB/(°/s) |

本章统一使用 ±16 g 和 ±2000 °/s：

```c
accel_g  = accel_raw / 2048.0f;
gyro_dps = gyro_raw / 16.4f;
```

先显示原始值，确认通信和拼接正确后，再显示物理量。不要在通信尚未通过时直接用换算结果判断传感器。

---

## 6. 实验一：软件 I2C 读取 WHO_AM_I

本实验只验证最短闭环，不读取六轴数据。

### 6.1 软件版 CubeMX 配置

软件版不启用 I2C2。四个 GPIO 都配置为开漏输出：

| 引脚 | User Label | Mode | 初始电平 | Pull |
| --- | --- | --- | --- | --- |
| PB8 | `OLED_SCL` | Output Open Drain | High | No pull |
| PB9 | `OLED_SDA` | Output Open Drain | High | No pull |
| PB10 | `MPU_SCL` | Output Open Drain | High | No pull |
| PB11 | `MPU_SDA` | Output Open Drain | High | No pull |

将前篇的 `OLED.c`、`OLED.h`、`OLED_Font.h` 和本章的 `MPU6050.c`、`MPU6050.h`、`MyI2C.c`、`MyI2C.h` 加入你自己的工程。这里只要求加入文件，不需要创建一套本教程专用工程。

### 6.2 软件 I2C 头文件和底层时序

`MyI2C.h` 至少声明：

```c
#ifndef __MY_I2C_H
#define __MY_I2C_H

#include <stdint.h>

void MyI2C_Init(void);
void MyI2C_Start(void);
void MyI2C_Stop(void);
void MyI2C_SendByte(uint8_t byte);
uint8_t MyI2C_ReceiveByte(void);
void MyI2C_SendAck(uint8_t ack_bit);
uint8_t MyI2C_ReceiveAck(void);

#endif
```

PB10/PB11 的引脚操作和延时：

```c
#include "MyI2C.h"
#include "main.h"

#define MYI2C_PORT     GPIOB
#define MYI2C_SCL_PIN  GPIO_PIN_10
#define MYI2C_SDA_PIN  GPIO_PIN_11

static void MyI2C_Delay(void)
{
    for (volatile uint32_t i = 0U; i < 40U; i++)
    {
        __NOP();
    }
}

static void MyI2C_W_SCL(uint8_t bit_value)
{
    HAL_GPIO_WritePin(MYI2C_PORT, MYI2C_SCL_PIN,
                      bit_value ? GPIO_PIN_SET : GPIO_PIN_RESET);
    MyI2C_Delay();
}

static void MyI2C_W_SDA(uint8_t bit_value)
{
    HAL_GPIO_WritePin(MYI2C_PORT, MYI2C_SDA_PIN,
                      bit_value ? GPIO_PIN_SET : GPIO_PIN_RESET);
    MyI2C_Delay();
}

static uint8_t MyI2C_R_SDA(void)
{
    uint8_t bit_value;

    bit_value = (HAL_GPIO_ReadPin(MYI2C_PORT, MYI2C_SDA_PIN)
                 == GPIO_PIN_SET) ? 1U : 0U;
    MyI2C_Delay();
    return bit_value;
}

void MyI2C_Init(void)
{
    MyI2C_W_SDA(1U);
    MyI2C_W_SCL(1U);
}
```

开漏输出写 1 表示释放线路。读取 SDA 前也必须先写 1；否则主机仍在拉低 SDA，从机发送的 1 会被覆盖。

实现六个基本时序函数：

```c
void MyI2C_Start(void)
{
    MyI2C_W_SDA(1U);
    MyI2C_W_SCL(1U);
    MyI2C_W_SDA(0U);
    MyI2C_W_SCL(0U);
}

void MyI2C_Stop(void)
{
    MyI2C_W_SDA(0U);
    MyI2C_W_SCL(1U);
    MyI2C_W_SDA(1U);
}

void MyI2C_SendByte(uint8_t byte)
{
    for (uint8_t i = 0U; i < 8U; i++)
    {
        MyI2C_W_SDA((byte & (0x80U >> i)) != 0U);
        MyI2C_W_SCL(1U);
        MyI2C_W_SCL(0U);
    }
}

uint8_t MyI2C_ReceiveByte(void)
{
    uint8_t byte = 0U;

    MyI2C_W_SDA(1U);
    for (uint8_t i = 0U; i < 8U; i++)
    {
        MyI2C_W_SCL(1U);
        if (MyI2C_R_SDA() != 0U)
        {
            byte |= (uint8_t)(0x80U >> i);
        }
        MyI2C_W_SCL(0U);
    }
    return byte;
}

void MyI2C_SendAck(uint8_t ack_bit)
{
    MyI2C_W_SDA(ack_bit);
    MyI2C_W_SCL(1U);
    MyI2C_W_SCL(0U);
}

uint8_t MyI2C_ReceiveAck(void)
{
    uint8_t ack_bit;

    MyI2C_W_SDA(1U);
    MyI2C_W_SCL(1U);
    ack_bit = MyI2C_R_SDA();
    MyI2C_W_SCL(0U);
    return ack_bit;
}
```

这里 `ack_bit=0` 表示 ACK，`ack_bit=1` 表示 NACK。最后一个接收字节后发送 `1U`，也就是 NACK。

### 6.3 软件版读取一个寄存器

```c
static HAL_StatusTypeDef MPU6050_SW_ReadRegs(
    uint8_t reg, uint8_t *data, uint16_t length)
{
    if ((data == NULL) || (length == 0U))
    {
        return HAL_ERROR;
    }

    MyI2C_Start();
    MyI2C_SendByte(MPU6050_ADDR_WRITE);
    if (MyI2C_ReceiveAck() != 0U) goto nack;

    MyI2C_SendByte(reg);
    if (MyI2C_ReceiveAck() != 0U) goto nack;

    MyI2C_Start();
    MyI2C_SendByte(MPU6050_ADDR_READ);
    if (MyI2C_ReceiveAck() != 0U) goto nack;

    for (uint16_t i = 0U; i < length; i++)
    {
        data[i] = MyI2C_ReceiveByte();
        MyI2C_SendAck((i + 1U == length) ? 1U : 0U);
    }

    MyI2C_Stop();
    return HAL_OK;

nack:
    MyI2C_Stop();
    return HAL_ERROR;
}
```

函数在地址、寄存器地址和读地址阶段都检查 ACK；任一步收到 NACK，就发送 STOP 并返回 `HAL_ERROR`。

为避免 `main.c` 直接调用 `static` 私有函数，在 `MPU6050.c` 中提供公开包装接口：

```c
HAL_StatusTypeDef MPU6050_ReadDeviceID(uint8_t *id)
{
    return MPU6050_SW_ReadRegs(MPU6050_WHO_AM_I, id, 1U);
}
```

### 6.4 最小主程序和验收

先只读取 `WHO_AM_I`：

```c
static uint8_t mpu_id;

HAL_Init();
SystemClock_Config();
MX_GPIO_Init();

OLED_Init();
MyI2C_Init();

if (MPU6050_ReadDeviceID(&mpu_id) == HAL_OK)
{
    OLED_ShowString(1, 1, "ID:");
    OLED_ShowHexNum(1, 4, mpu_id, 2U);
}
else
{
    OLED_ShowString(1, 1, "READ ERR");
}
```

预期现象：OLED 显示 `ID:68`。

验收条件：

- PB10/PB11 空闲为高；
- 软件 I2C 线上出现 `0xD0 -> 0x75 -> 重复起始 -> 0xD1`；
- `WHO_AM_I` 返回 `0x68`；
- OLED 稳定显示 `ID:68`。

若失败，先只检查供电、共地、AD0、地址字节和重复起始，不要先修改六轴解析代码。

---

## 7. 实验二：配置并读取六轴原始数据

### 7.1 共用上层数据接口

软件版和硬件版都可以向 `main.c` 提供相同的数据结构：

```c
typedef struct
{
    int16_t AccX;
    int16_t AccY;
    int16_t AccZ;
    int16_t GyroX;
    int16_t GyroY;
    int16_t GyroZ;
} MPU6050_Data;

HAL_StatusTypeDef MPU6050_Init(I2C_HandleTypeDef *hi2c);
HAL_StatusTypeDef MPU6050_ReadDeviceID(uint8_t *id);
HAL_StatusTypeDef MPU6050_GetData(MPU6050_Data *data);
```

在教学上，软件版可以传 `NULL` 表示使用 GPIO 底层，硬件版传 `&hi2c2` 表示使用 HAL 底层。这里的 `NULL` 只是课程封装的选择方式，不代表软件版真的使用了硬件 I2C 句柄。

### 7.2 软件版初始化

软件版初始化的顺序是：

```text
MyI2C_Init()
    -> 读取 WHO_AM_I
    -> 写 PWR_MGMT_1
    -> 写 PWR_MGMT_2
    -> 写 SMPLRT_DIV
    -> 写 CONFIG
    -> 写 GYRO_CONFIG
    -> 写 ACCEL_CONFIG
```

在 `MPU6050.c` 中，用同一组软件事务封装公开接口。下面是教学版的最小实现；`MPU6050_SW_WriteReg()` 与 `MPU6050_SW_ReadRegs()` 都在该文件内，`main.c` 只调用公开函数：

```c
static HAL_StatusTypeDef MPU6050_SW_WriteReg(
    uint8_t reg, uint8_t value)
{
    MyI2C_Start();
    MyI2C_SendByte(MPU6050_ADDR_WRITE);
    if (MyI2C_ReceiveAck() != 0U) goto nack;

    MyI2C_SendByte(reg);
    if (MyI2C_ReceiveAck() != 0U) goto nack;

    MyI2C_SendByte(value);
    if (MyI2C_ReceiveAck() != 0U) goto nack;

    MyI2C_Stop();
    return HAL_OK;

nack:
    MyI2C_Stop();
    return HAL_ERROR;
}

HAL_StatusTypeDef MPU6050_Init(I2C_HandleTypeDef *hi2c)
{
    uint8_t id;

    (void)hi2c;
    MyI2C_Init();

    if (MPU6050_SW_ReadRegs(MPU6050_WHO_AM_I, &id, 1U) != HAL_OK)
        return HAL_ERROR;
    if (id != 0x68U)
        return HAL_ERROR;

    if (MPU6050_SW_WriteReg(
            MPU6050_PWR_MGMT_1, MPU6050_PWR_MGMT_1_VALUE) != HAL_OK)
        return HAL_ERROR;
    if (MPU6050_SW_WriteReg(
            MPU6050_PWR_MGMT_2, MPU6050_PWR_MGMT_2_VALUE) != HAL_OK)
        return HAL_ERROR;
    if (MPU6050_SW_WriteReg(
            MPU6050_SMPLRT_DIV, MPU6050_SMPLRT_DIV_VALUE) != HAL_OK)
        return HAL_ERROR;
    if (MPU6050_SW_WriteReg(
            MPU6050_CONFIG, MPU6050_CONFIG_VALUE) != HAL_OK)
        return HAL_ERROR;
    if (MPU6050_SW_WriteReg(
            MPU6050_GYRO_CONFIG, MPU6050_GYRO_FS_2000DPS) != HAL_OK)
        return HAL_ERROR;

    return MPU6050_SW_WriteReg(
        MPU6050_ACCEL_CONFIG, MPU6050_ACCEL_FS_16G);
}
```

每次写寄存器后都检查 `MyI2C_ReceiveAck()`。若任一步返回 NACK，发送 STOP 并返回 `HAL_ERROR`。初始化成功后再读取六轴数据。

### 7.3 主循环显示

```c
static MPU6050_Data mpu_data;

if (MPU6050_Init(NULL) != HAL_OK)
{
    OLED_ShowString(1, 7, "I2C ERR");
    Error_Handler();
}

while (1)
{
    if (MPU6050_GetData(&mpu_data) == HAL_OK)
    {
        OLED_ShowSignedNum(2, 1, mpu_data.AccX, 5U);
        OLED_ShowSignedNum(3, 1, mpu_data.AccY, 5U);
        OLED_ShowSignedNum(4, 1, mpu_data.AccZ, 5U);

        OLED_ShowSignedNum(2, 8, mpu_data.GyroX, 5U);
        OLED_ShowSignedNum(3, 8, mpu_data.GyroY, 5U);
        OLED_ShowSignedNum(4, 8, mpu_data.GyroZ, 5U);
    }
    else
    {
        OLED_ShowString(1, 7, "READ ERR");
    }

    HAL_Delay(50U);
}
```

### 7.4 预期现象与验收

- 第一行显示 `ID:68`；
- 第二到四行左侧为 AccX/Y/Z，右侧为 GyroX/Y/Z；
- 静止水平放置时，某一加速度轴绝对值接近 `2048 LSB`，约为 1 g；
- 静止时陀螺仪接近 0，但允许存在零偏；
- 转动模块时，对应陀螺仪轴变化明显；
- 翻转模块时，对应加速度轴正负号改变。

“数字会变化”不能单独证明设备身份正确，必须先通过 `WHO_AM_I=0x68`。

---

## 8. 实验三：从逐寄存器读取迁移到 14 字节连续读取

### 8.1 先用逐寄存器方式理解拼接

教学上可以先分别读取高、低字节：

```c
uint8_t pair[2];

if (MPU6050_SW_ReadRegs(
        MPU6050_ACCEL_XOUT_H, pair, sizeof(pair)) == HAL_OK)
{
    AccX = MPU6050_BE16(pair);
}
```

这里一次读取 X 轴的高、低字节；如果要严格观察两个独立寄存器事务，也可以分别以 `0x3B` 和 `0x3C` 为起始地址读取。

这样最容易看清“寄存器地址 -> 高字节 -> 低字节 -> 16 位数”的关系，但一轮六轴读取会产生很多次完整事务。

### 8.2 改为连续读取

将起始地址设为 `ACCEL_XOUT_H(0x3B)`，一次读取 14 字节，再按第 5.3 节的偏移解析。软件版复用 `MPU6050_SW_ReadRegs()`，硬件版复用 `MPU6050_HW_ReadRegs()`；对 `main.c` 暴露的 `MPU6050_GetData()` 来说，底层函数可以保持私有。

```c
static HAL_StatusTypeDef MPU6050_SW_GetData(MPU6050_Data *data)
{
    uint8_t buffer[14];
    HAL_StatusTypeDef status;

    if (data == NULL)
        return HAL_ERROR;

    status = MPU6050_SW_ReadRegs(
        MPU6050_ACCEL_XOUT_H, buffer, sizeof(buffer));
    if (status != HAL_OK)
        return status;

    data->AccX  = MPU6050_BE16(&buffer[0]);
    data->AccY  = MPU6050_BE16(&buffer[2]);
    data->AccZ  = MPU6050_BE16(&buffer[4]);
    data->GyroX = MPU6050_BE16(&buffer[8]);
    data->GyroY = MPU6050_BE16(&buffer[10]);
    data->GyroZ = MPU6050_BE16(&buffer[12]);
    return HAL_OK;
}
```

硬件版只需将 `MPU6050_SW_ReadRegs()` 换成 `MPU6050_HW_ReadRegs()`，并保留同一组偏移和数据结构。

连续读取的验收不是“数值必须完全相同”，而是：

- 六个轴仍能按相同位置显示；
- 姿态变化趋势不变；
- 逻辑分析仪看到中间字节后 ACK，最后一个字节后 NACK 和 STOP；
- 一轮数据读取的事务数量减少。

---

## 9. 实验四：用 HAL 硬件 I2C 替换底层

### 9.1 硬件版 CubeMX 配置

保留 PB8/PB9 的 OLED 软件 I2C 配置，再启用：

1. `Connectivity -> I2C2 -> I2C`；
2. PB10 自动配置为 `I2C2_SCL`；
3. PB11 自动配置为 `I2C2_SDA`；
4. Speed Mode 选择 Standard Mode；
5. Clock Speed 设置为 `100000 Hz`；
6. 使用 7 位地址模式。

生成代码后应存在：

```c
I2C_HandleTypeDef hi2c2;
void MX_I2C2_Init(void);
```

PB10/PB11 由 HAL MSP 配置为复用开漏，不要再在 `MX_GPIO_Init()` 中把它们改成普通输出。软件版和硬件版应分别在各自工程中配置，不能同时把同一工程的 PB10/PB11 配成两种模式。

### 9.2 HAL 寄存器读写

```c
#define MPU6050_ADDR_HAL     (0x68U << 1)
#define MPU6050_TIMEOUT_MS   100U

static I2C_HandleTypeDef *g_mpu_i2c;

static HAL_StatusTypeDef MPU6050_HW_WriteReg(
    uint8_t reg, uint8_t value)
{
    return HAL_I2C_Mem_Write(
        g_mpu_i2c,
        MPU6050_ADDR_HAL,
        reg,
        I2C_MEMADD_SIZE_8BIT,
        &value,
        1U,
        MPU6050_TIMEOUT_MS);
}

static HAL_StatusTypeDef MPU6050_HW_ReadRegs(
    uint8_t reg, uint8_t *data, uint16_t length)
{
    if ((data == NULL) || (length == 0U))
    {
        return HAL_ERROR;
    }

    return HAL_I2C_Mem_Read(
        g_mpu_i2c,
        MPU6050_ADDR_HAL,
        reg,
        I2C_MEMADD_SIZE_8BIT,
        data,
        length,
        MPU6050_TIMEOUT_MS);
}
```

`HAL_I2C_Mem_Write()` 的 `pData` 必须是有效缓冲区地址，不能把 `0x00`、`0x07` 等寄存器值直接当作指针传入。这里使用局部变量 `value`，再传 `&value`。

`I2C_MEMADD_SIZE_8BIT` 表示 MPU6050 内部寄存器地址宽度为 8 位。

### 9.3 硬件版初始化和主程序

硬件版初始化先保存句柄，再用有限超时检查设备和寄存器：

```c
HAL_StatusTypeDef MPU6050_Init(I2C_HandleTypeDef *hi2c)
{
    uint8_t id;

    if (hi2c == NULL)
    {
        return HAL_ERROR;
    }
    g_mpu_i2c = hi2c;

    if (HAL_I2C_IsDeviceReady(
            g_mpu_i2c, MPU6050_ADDR_HAL, 3U,
            MPU6050_TIMEOUT_MS) != HAL_OK)
    {
        return HAL_ERROR;
    }

    if (MPU6050_HW_ReadRegs(MPU6050_WHO_AM_I, &id, 1U) != HAL_OK)
        return HAL_ERROR;
    if (id != 0x68U)
        return HAL_ERROR;

    if (MPU6050_HW_WriteReg(
            MPU6050_PWR_MGMT_1, MPU6050_PWR_MGMT_1_VALUE) != HAL_OK)
        return HAL_ERROR;
    if (MPU6050_HW_WriteReg(
            MPU6050_PWR_MGMT_2, MPU6050_PWR_MGMT_2_VALUE) != HAL_OK)
        return HAL_ERROR;
    if (MPU6050_HW_WriteReg(
            MPU6050_SMPLRT_DIV, MPU6050_SMPLRT_DIV_VALUE) != HAL_OK)
        return HAL_ERROR;
    if (MPU6050_HW_WriteReg(
            MPU6050_CONFIG, MPU6050_CONFIG_VALUE) != HAL_OK)
        return HAL_ERROR;
    if (MPU6050_HW_WriteReg(
            MPU6050_GYRO_CONFIG, MPU6050_GYRO_FS_2000DPS) != HAL_OK)
        return HAL_ERROR;

    return MPU6050_HW_WriteReg(
        MPU6050_ACCEL_CONFIG, MPU6050_ACCEL_FS_16G);
}
```

主程序只改变初始化链，显示和主循环保持不变：

```c
HAL_Init();
SystemClock_Config();
MX_GPIO_Init();
MX_I2C2_Init();

OLED_Init();
OLED_ShowString(1, 1, "ID:");

if (MPU6050_Init(&hi2c2) != HAL_OK)
{
    OLED_ShowString(1, 7, "I2C ERR");
    Error_Handler();
}
```

### 9.4 软硬件对照验收

| 对比项 | 软件 I2C | HAL 硬件 I2C |
| --- | --- | --- |
| MPU6050 引脚 | PB10/PB11 GPIO | PB10/PB11 I2C2 |
| 时序产生 | CPU 调用 GPIO | I2C2 外设 |
| 寄存器接口 | `MyI2C_*` 组合事务 | `HAL_I2C_Mem_*` |
| OLED | PB8/PB9 软件 I2C | PB8/PB9 软件 I2C |
| 上层输出 | `ID:68` + 六轴原始值 | `ID:68` + 六轴原始值 |
| CPU 占用 | 较高 | 较低 |
| 波形 | 受代码延时影响 | 通常更规整 |

保持接线、姿态、显示代码和读取周期一致后，两边的符号、量级和变化趋势应一致；瞬时值会受噪声、零偏和采样时刻影响，不要求逐位相同。

---

## 10. 分层验收

### 10.1 电气层

- 两个模块供电正常并共地；
- PB8/PB9、PB10/PB11 空闲时都为高；
- 主机拉低时能接近 0 V；
- 上升沿能在下一个采样时刻前恢复到有效高电平。

### 10.2 地址层

- 软件版发送 `0xD0` 后收到 ACK；
- 硬件版 `HAL_I2C_IsDeviceReady(&hi2c2, 0x68 << 1, ...)` 返回 `HAL_OK`；
- 失败时根据 AD0 检查 `0x68/0x69`，不要同时随意修改 `WHO_AM_I` 的期望值。

### 10.3 身份层

- `WHO_AM_I(0x75)` 返回 `0x68`；
- OLED 第一行稳定显示 `ID:68`；
- 地址应答正常但 ID 错误时，检查寄存器地址、重复起始和读方向。

### 10.4 数据层

- 第二到四行同时显示六个有符号值；
- 静止时陀螺仪接近 0，但允许存在零偏；
- 水平静止时某一加速度轴绝对值接近 `2048 LSB`；
- 沿单轴旋转时对应陀螺仪轴变化最明显；
- 翻转模块时对应加速度轴正负改变。

### 10.5 波形层

读取 `WHO_AM_I` 时，应看到：

```text
START -> 0x68/W -> ACK -> 0x75 -> ACK
      -> RESTART -> 0x68/R -> ACK -> 0x68 -> NACK -> STOP
```

连续读取 14 字节时，中间 13 个字节后主机发送 ACK，最后一个字节后发送 NACK 和 STOP。

> 待确认项：实际总线频率、上升时间、上拉电阻和模块是否为真正的 MPU6050，需要以逻辑分析仪/示波器、模块资料和实测结果为准。本文的代码链路检查不能替代硬件测试。

---

## 11. 按现象故障定位

### 11.1 OLED 不亮

先回到前篇，单独运行 `OLED_Init()` 和 `OLED_ShowString()`。OLED 自身尚未通过验收时，不要用它判断 MPU6050 是否正常。

### 11.2 MPU6050 没有 ACK

按顺序检查：

1. VCC、GND 和共地；
2. PB10=SCL、PB11=SDA；
3. 空闲高电平和上拉；
4. AD0 对应地址 `0x68/0x69`；
5. 软件版是否发送 `0xD0`，而不是 `0x68`；
6. HAL 是否传入 `0x68 << 1`，且没有重复左移；
7. 软件版第 9 个时钟前是否释放 SDA。

### 11.3 `WHO_AM_I` 为 `0xFF`

通常表示 SDA 始终为高或从机没有驱动。先查地址、SDA 接线、重复起始、读方向和上拉。

### 11.4 `WHO_AM_I` 为 `0x00`

通常表示 SDA 始终为低。检查短路、主机是否释放 SDA、从机是否卡住，以及读取代码是否一直把低电平拼入结果。

### 11.5 ID 正确，但六轴不变化

- **先检查**：是否写入 `PWR_MGMT_1=0x01`；
- **再检查**：读取起始地址是否为 `0x3B`，长度是否为 14；
- **继续检查**：高低字节拼接、主循环调用和 OLED 固定宽度覆盖；
- **结论**：身份层通过后，优先查配置、数据地址和解析，不要再反复改设备地址。

### 11.6 数值相差约 8 倍

两套工程都应向 `GYRO_CONFIG` 和 `ACCEL_CONFIG` 写入 `0x18`。若对应数据仍相差约 8 倍，可能是一套工程没有成功写入配置、下载的是旧程序，或仍处于默认量程。回读两个寄存器，确认它们均为 `0x18`。

### 11.7 数值相差 256 倍或负数变成大正数

检查高字节是否左移 8 位，以及最终变量是否为 `int16_t`：

```c
value = (int16_t)(((uint16_t)high << 8) | low);
```

### 11.8 硬件 I2C 失败，软件 I2C 成功

检查 I2C2 是否启用、PB10/PB11 是否为复用开漏、APB1 时钟和速度是否正确、HAL 返回值是 `HAL_BUSY`、`HAL_TIMEOUT` 还是 `HAL_ERROR`，以及 GPIO 初始化是否覆盖了 I2C2 引脚配置。

### 11.9 软件 I2C 失败，硬件 I2C 成功

检查 START/STOP 边沿、最高位先发、ACK 第 9 个时钟、读前释放 SDA、最后一个字节 NACK，以及空循环延时是否因优化或系统时钟变化而过短。

### 11.10 程序卡在通信函数

- HAL 调用必须使用有限超时，不要全部传 `HAL_MAX_DELAY`；
- 软件 I2C 课程版没有完整的时钟拉伸超时，SCL 被持续拉低时可能无法自行退出；
- 总线异常后可以释放 SDA、产生最多 9 个 SCL 脉冲，再发送 STOP；
- 总线恢复不能修复短路、错误供电或损坏的模块。

软件 I2C 的空循环延时只适合入门复现实验，正式项目应增加可测的定时、拉伸等待、超时和并发保护。

---

## 12. 自测题与参考答案

### 12.1 自测题

1. 为什么软件 I2C 发送 `0xD0/0xD1`，而 HAL 接口写成 `0x68 << 1`？
2. 指定地址读为什么需要重复起始？
3. 读取最后一个字节后为什么发送 NACK？
4. MPU6050 上电后为什么必须先配置 `PWR_MGMT_1`？
5. 高字节为什么要左移 8 位？
6. 软件版和硬件版参数统一后，为什么瞬时原始值仍不会完全相等？
7. 为什么应先验收 `WHO_AM_I`，再调试六轴和 OLED 布局？
8. 14 字节连续读取相对逐寄存器读取解决了什么问题？
9. 为什么读前必须释放 SDA？
10. 软件版和硬件版分别应该在什么位置改变代码？

### 12.2 参考答案

1. 两者最终都把 7 位地址放到地址字节的高 7 位；软件代码直接构造线上字节，HAL 要求传入左移后的地址参数。
2. 前半段用写方向设置内部寄存器地址，后半段保持总线控制权并切换到读方向。
3. NACK 告诉从机当前字节是最后一个，随后主机发送 STOP 结束事务。
4. MPU6050 默认睡眠，不解除睡眠就不能正常更新测量数据。
5. 大端数据的高字节对应 bit15 到 bit8，必须左移 8 位后再与低字节拼接。
6. 两次读取的采样时刻不同，还存在噪声、零偏和姿态误差；统一参数保证的是灵敏度和量级一致。
7. `WHO_AM_I` 是最短的“供电 -> 地址 -> 寄存器读”闭环；它失败时继续看六轴和显示只会扩大排查范围。
8. 它减少事务开销和一轮读取的时间跨度，并降低跨数据更新边界读取的风险。
9. SDA 由从机发送时，主机必须停止拉低线路，否则从机发送的高电平会被主机覆盖。
10. 软件版改变 `MyI2C_*` 组合事务和 GPIO 配置；硬件版改变 I2C2 配置与 `HAL_I2C_Mem_*` 底层，上层数据结构、显示和验收保持不变。

当软件版和硬件版都能显示 `ID:68`，第二到四行按左加速度、右陀螺仪排列，改变姿态时六项数据按预期变化，并且逻辑分析仪能看到指定地址读的完整序列，本章实验闭环就完成了。固定周期采样、零偏标定和姿态融合属于后续可选扩展，不是本章通过条件。
